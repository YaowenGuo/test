apply plugin: 'java-library'

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }

    test{
        java { srcDirs=['src'] }
    }
}



dependencies {
    implementation fileTree(dir: "src", include: ["*.jar"])
}