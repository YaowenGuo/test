命名空间和变量作用域

    命名空间是对对象索引的的管理方式，是名称到对象的映射。
    python有三种命名空间：局部，全局，内置。
    命名空间的查找顺序是 局部，全局，内置，如果都没找到，
    则抛出错误，找不到该变量。
    
    
    变量作用域是指对象引用能被访问到的区域。对象的作用域
    一般是包好该变量的代码块。
    局部变量的能够覆盖全局变量，使其在该作用域内不可见。
    想要在局部声明全局变量使用global修饰即可。即可讲其作用
    域变成全局。
